
export const Footer = () => {
    return `
    <div>
      <input type="checkbox" id= "posts" name="foot" checked>
      <label for="Posts Since">Posts Since</label>

      <input type="checkbox" id= "userss" name="foot" checked>
      <label for="foot__userPosts">Posts by User</label>

      <input type="checkbox" id= "userss" name="foot" checked>
      <label for="foot__favorites">Show Only Favorites</label>
      </div>
    `
}